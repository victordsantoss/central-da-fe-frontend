import { Box } from '@mui/material';

export const SupplierAdvancedFilterView = () => {
  return (
    <Box display={'flex'} flexDirection={'column'} gap={{ xs: 1, md: 2 }}>
      FILTROS AVANÇADOS DOS FORNECEDORES
    </Box>
  );
};
